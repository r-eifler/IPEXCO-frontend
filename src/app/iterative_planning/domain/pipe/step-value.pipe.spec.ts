import { StepValuePipe } from './step-value.pipe';

describe('StepValuePipe', () => {
  it('create an instance', () => {
    const pipe = new StepValuePipe();
    expect(pipe).toBeTruthy();
  });
});
