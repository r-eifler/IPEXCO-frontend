import { StepStatusColorPipe } from './step-status-color.pipe';

describe('StepStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new StepStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
