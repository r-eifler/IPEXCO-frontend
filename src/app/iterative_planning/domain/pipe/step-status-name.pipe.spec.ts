import { StepStatusNamePipe } from './step-status-name.pipe';

describe('StepStatusNamePipe', () => {
  it('create an instance', () => {
    const pipe = new StepStatusNamePipe();
    expect(pipe).toBeTruthy();
  });
});
