export interface ExampleProject {
    name: string;
    domainName: string;
    description: string;
    domainFile: string;
    problemFile: string;
    templateFile: string;
    promptFile: string;
  }